<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Users - Admin Panel - SynzBin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white min-h-screen">
    <nav class="bg-gray-800 border-b border-gray-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center space-x-4">
                    <a href="/" class="text-2xl font-bold">
                        <span class="text-red-400">{</span>SynzBin<span class="text-red-400">}</span>
                    </a>
                    <span class="px-3 py-1 bg-red-500 text-xs rounded-full">ADMIN</span>
                </div>
                <div class="flex items-center space-x-6">
                    <a href="/panel-a8f5e9c2b7" class="text-gray-300 hover:text-white transition">Dashboard</a>
                    <a href="/panel-a8f5e9c2b7/users" class="text-white font-semibold">Users</a>
                    <a href="/panel-a8f5e9c2b7/pastes" class="text-gray-300 hover:text-white transition">Pastes</a>
                    <a href="/panel-a8f5e9c2b7/system" class="text-gray-300 hover:text-white transition">System</a>
                    <a href="/" class="text-gray-400 hover:text-gray-300 transition">Exit Admin</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-3xl font-bold">Manage Users</h1>

            <form action="/panel-a8f5e9c2b7/users/search" method="GET" class="flex gap-2">
                <input type="text" name="q" placeholder="Search users..."
                    value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
                    class="px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-red-400 text-white">
                <button type="submit" class="px-6 py-2 bg-red-500 hover:bg-red-600 rounded-lg transition">
                    Search
                </button>
                <% if (typeof searchQuery !=='undefined' && searchQuery) { %>
                    <a href="/panel-a8f5e9c2b7/users"
                        class="px-6 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg transition">
                        Clear
                    </a>
                    <% } %>
            </form>
        </div>

        <div class="bg-gray-800 rounded-lg p-6 border border-gray-700 mb-6">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-gray-400 text-sm">Total Users</p>
                    <p class="text-3xl font-bold">
                        <%= totalUsers %>
                    </p>
                </div>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-blue-400" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
                </svg>
            </div>
        </div>

        <div class="bg-gray-800 rounded-lg border border-gray-700 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-700">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                User</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Discord ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Role</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Joined</th>
                            <th class="px-6 py-3 text-right text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-700">
                        <% if (users.length> 0) { %>
                            <% users.forEach(u=> { %>
                                <tr class="hover:bg-gray-700 transition">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <img src="https://cdn.discordapp.com/avatars/<%= u.discordId %>/<%= u.avatar %>.png"
                                                alt="<%= u.username %>" class="w-10 h-10 rounded-full mr-3">
                                            <div>
                                                <p class="font-semibold">
                                                    <%= u.username %>
                                                </p>
                                                <p class="text-sm text-gray-400">
                                                    <%= u.email || 'No email' %>
                                                </p>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <code class="text-sm text-gray-400"><%= u.discordId %></code>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <% if (user.role==='owner' ) { %>
                                            <select data-user-id="<%= u.discordId %>"
                                                class="role-select px-3 py-1 bg-gray-700 border border-gray-600 rounded text-sm focus:outline-none focus:border-red-400"
                                                <%=u.role==='owner' ? 'disabled' : '' %>>
                                                <option value="user" <%=u.role==='user' ? 'selected' : '' %>>User
                                                </option>
                                                <option value="vip" <%=u.role==='vip' ? 'selected' : '' %>>VIP</option>
                                                <option value="admin" <%=u.role==='admin' ? 'selected' : '' %>>Admin
                                                </option>
                                                <option value="owner" <%=u.role==='owner' ? 'selected' : '' %>>Owner
                                                </option>
                                            </select>
                                            <% } else { %>
                                                <span class="px-3 py-1 rounded text-xs font-semibold
                                                <%= u.role === 'owner' ? 'bg-purple-500' : '' %>
                                                <%= u.role === 'admin' ? 'bg-red-500' : '' %>
                                                <%= u.role === 'vip' ? 'bg-yellow-500' : '' %>
                                                <%= u.role === 'user' ? 'bg-gray-600' : '' %>">
                                                    <%= u.role.toUpperCase() %>
                                                </span>
                                                <% } %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <% if (u.banned) { %>
                                            <span
                                                class="px-3 py-1 bg-red-500 rounded text-xs font-semibold">BANNED</span>
                                            <% } else { %>
                                                <span
                                                    class="px-3 py-1 bg-green-500 rounded text-xs font-semibold">ACTIVE</span>
                                                <% } %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-400">
                                        <%= new Date(u.createdAt).toLocaleDateString() %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm">
                                        <% if (u.role !=='owner' && u.discordId !==user.discordId) { %>
                                            <div class="flex justify-end gap-2">
                                                <a href="/user/<%= u.discordId %>"
                                                    class="text-blue-400 hover:text-blue-300 transition">
                                                    View
                                                </a>
                                                <button
                                                    onclick="toggleBan('<%= u.discordId %>', '<%= u.banned ? 'false' : 'true' %>')"
                                                    class="<%= u.banned ? 'text-green-400 hover:text-green-300' : 'text-yellow-400 hover:text-yellow-300' %> transition">
                                                    <%= u.banned ? 'Unban' : 'Ban' %>
                                                </button>
                                                <button onclick="deleteUser('<%= u.discordId %>', '<%= u.username %>')"
                                                    class="text-red-400 hover:text-red-300 transition">
                                                    Delete
                                                </button>
                                            </div>
                                            <% } else { %>
                                                <a href="/user/<%= u.discordId %>"
                                                    class="text-blue-400 hover:text-blue-300 transition">
                                                    View
                                                </a>
                                                <% } %>
                                    </td>
                                </tr>
                                <% }); %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="6" class="px-6 py-8 text-center text-gray-400">
                                                No users found
                                            </td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Pagination -->
        <% if (totalPages> 1) { %>
            <div class="mt-6 flex justify-center gap-2">
                <% if (currentPage> 1) { %>
                    <a href="?page=<%= currentPage - 1 %>"
                        class="px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg hover:bg-gray-700 transition">
                        Previous
                    </a>
                    <% } %>

                        <% for (let i=1; i <=totalPages; i++) { %>
                            <% if (i===currentPage) { %>
                                <span class="px-4 py-2 bg-red-500 rounded-lg font-semibold">
                                    <%= i %>
                                </span>
                                <% } else if (i===1 || i===totalPages || (i>= currentPage - 2 && i <= currentPage + 2))
                                        { %>
                                        <a href="?page=<%= i %>"
                                            class="px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg hover:bg-gray-700 transition">
                                            <%= i %>
                                        </a>
                                        <% } else if (i===currentPage - 3 || i===currentPage + 3) { %>
                                            <span class="px-4 py-2 text-gray-500">...</span>
                                            <% } %>
                                                <% } %>

                                                    <% if (currentPage < totalPages) { %>
                                                        <a href="?page=<%= currentPage + 1 %>"
                                                            class="px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg hover:bg-gray-700 transition">
                                                            Next
                                                        </a>
                                                        <% } %>
            </div>
            <% } %>
    </div>

    <script src="/js/admin.js"></script>
</body>

</html>