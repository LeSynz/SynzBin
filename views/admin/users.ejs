<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Users - Admin Panel - SynzBin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/js/dropdown.js" defer></script>
</head>

<body class="bg-gray-900 text-white min-h-screen">
    <nav class="bg-gray-800 border-b border-gray-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="text-xl md:text-2xl font-bold">
                        <span class="text-red-400">{</span>SynzBin<span class="text-red-400">}</span>
                    </a>
                    <span class="ml-3 md:ml-4 px-2 md:px-3 py-1 bg-red-500 text-xs rounded-full">ADMIN</span>
                </div>

                <div class="hidden md:flex items-center space-x-6">
                    <a href="/panel-a8f5e9c2b7" class="text-gray-300 hover:text-white transition">Dashboard</a>
                    <a href="/panel-a8f5e9c2b7/users" class="text-white font-semibold transition">Users</a>
                    <a href="/panel-a8f5e9c2b7/pastes" class="text-gray-300 hover:text-white transition">Pastes</a>
                    <a href="/panel-a8f5e9c2b7/system" class="text-gray-300 hover:text-white transition">System</a>
                    <a href="/user/<%= user.discordId %>" class="text-gray-300 hover:text-white transition">My
                        Profile</a>
                    <a href="/" class="text-gray-400 hover:text-gray-300 transition">Exit Admin</a>
                </div>

                <div class="md:hidden flex items-center relative">
                    <button id="mobile-menu-button" class="text-gray-300 hover:text-white focus:outline-none">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
                            stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                    <div id="mobile-menu"
                        class="hidden absolute right-0 top-12 w-56 bg-gray-800 rounded-lg shadow-lg border border-gray-700 py-2 z-50">
                        <a href="/panel-a8f5e9c2b7"
                            class="flex items-center text-gray-300 hover:text-white px-4 py-2 hover:bg-gray-700 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                            </svg>
                            Dashboard
                        </a>
                        <a href="/panel-a8f5e9c2b7/users"
                            class="flex items-center text-white font-semibold px-4 py-2 hover:bg-gray-700 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
                            </svg>
                            Users
                        </a>
                        <a href="/panel-a8f5e9c2b7/pastes"
                            class="flex items-center text-gray-300 hover:text-white px-4 py-2 hover:bg-gray-700 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                            </svg>
                            Pastes
                        </a>
                        <a href="/panel-a8f5e9c2b7/system"
                            class="flex items-center text-gray-300 hover:text-white px-4 py-2 hover:bg-gray-700 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z" />
                            </svg>
                            System
                        </a>
                        <div class="border-t border-gray-700 my-2"></div>
                        <a href="/user/<%= user.discordId %>"
                            class="flex items-center text-gray-300 hover:text-white px-4 py-2 hover:bg-gray-700 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                            My Profile
                        </a>
                        <a href="/"
                            class="flex items-center text-gray-400 hover:text-gray-300 px-4 py-2 hover:bg-gray-700 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                            </svg>
                            Exit Admin
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 md:py-8">
        <div class="flex flex-col md:flex-row md:justify-between md:items-center mb-6 md:mb-8 gap-4">
            <h1 class="text-2xl md:text-3xl font-bold">Manage Users</h1>

            <form action="/panel-a8f5e9c2b7/users/search" method="GET" class="flex flex-col sm:flex-row gap-2">
                <input type="text" name="q" placeholder="Search users..."
                    value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
                    class="px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-red-400 text-white text-sm md:text-base w-full sm:w-auto">
                <div class="flex gap-2">
                    <button type="submit"
                        class="flex-1 sm:flex-none px-4 md:px-6 py-2 bg-red-500 hover:bg-red-600 rounded-lg transition text-sm md:text-base">
                        Search
                    </button>
                    <% if (typeof searchQuery !=='undefined' && searchQuery) { %>
                        <a href="/panel-a8f5e9c2b7/users"
                            class="flex-1 sm:flex-none px-4 md:px-6 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg transition text-center text-sm md:text-base">
                            Clear
                        </a>
                        <% } %>
                </div>
            </form>
        </div>

        <div class="bg-gray-800 rounded-lg p-4 md:p-6 border border-gray-700 mb-4 md:mb-6">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-gray-400 text-xs md:text-sm">Total Users</p>
                    <p class="text-2xl md:text-3xl font-bold">
                        <%= totalUsers %>
                    </p>
                </div>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 md:h-12 md:w-12 text-blue-400" fill="none"
                    viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
                </svg>
            </div>
        </div>

        <div class="hidden lg:block bg-gray-800 rounded-lg border border-gray-700 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-700">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                User</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Discord ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Role</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Joined</th>
                            <th class="px-6 py-3 text-right text-xs font-medium text-gray-300 uppercase tracking-wider">
                                Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-700">
                        <% if (users.length> 0) { %>
                            <% users.forEach(u=> { %>
                                <tr class="hover:bg-gray-700 transition">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <img src="https://cdn.discordapp.com/avatars/<%= u.discordId %>/<%= u.avatar %>.png"
                                                alt="<%= u.username %>" class="w-10 h-10 rounded-full mr-3">
                                            <div>
                                                <p class="font-semibold">
                                                    <%= u.username %>
                                                </p>
                                                <p class="text-sm text-gray-400">
                                                    <%= u.email || 'No email' %>
                                                </p>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <code class="text-sm text-gray-400"><%= u.discordId %></code>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <% if (user.role==='owner' ) { %>
                                            <select data-user-id="<%= u.discordId %>"
                                                class="role-select px-3 py-1 bg-gray-700 border border-gray-600 rounded text-sm focus:outline-none focus:border-red-400"
                                                <%=u.role==='owner' ? 'disabled' : '' %>>
                                                <option value="user" <%=u.role==='user' ? 'selected' : '' %>>User
                                                </option>
                                                <option value="vip" <%=u.role==='vip' ? 'selected' : '' %>>VIP</option>
                                                <option value="admin" <%=u.role==='admin' ? 'selected' : '' %>>Admin
                                                </option>
                                                <option value="owner" <%=u.role==='owner' ? 'selected' : '' %>>Owner
                                                </option>
                                            </select>
                                            <% } else { %>
                                                <span class="px-3 py-1 rounded text-xs font-semibold
                                            <%= u.role === 'owner' ? 'bg-purple-500' : '' %>
                                            <%= u.role === 'admin' ? 'bg-red-500' : '' %>
                                            <%= u.role === 'vip' ? 'bg-yellow-500' : '' %>
                                            <%= u.role === 'user' ? 'bg-gray-600' : '' %>">
                                                    <%= u.role.toUpperCase() %>
                                                </span>
                                                <% } %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <% if (u.banned) { %>
                                            <span
                                                class="px-3 py-1 bg-red-500 rounded text-xs font-semibold">BANNED</span>
                                            <% } else { %>
                                                <span
                                                    class="px-3 py-1 bg-green-500 rounded text-xs font-semibold">ACTIVE</span>
                                                <% } %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-400">
                                        <%= new Date(u.createdAt).toLocaleDateString() %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm">
                                        <% if (u.role !=='owner' && u.discordId !==user.discordId) { %>
                                            <div class="flex justify-end gap-2">
                                                <a href="/user/<%= u.discordId %>"
                                                    class="text-blue-400 hover:text-blue-300 transition">View</a>
                                                <button
                                                    onclick="toggleBan('<%= u.discordId %>', '<%= u.banned ? 'false' : 'true' %>')"
                                                    class="<%= u.banned ? 'text-green-400 hover:text-green-300' : 'text-yellow-400 hover:text-yellow-300' %> transition">
                                                    <%= u.banned ? 'Unban' : 'Ban' %>
                                                </button>
                                                <button onclick="deleteUser('<%= u.discordId %>', '<%= u.username %>')"
                                                    class="text-red-400 hover:text-red-300 transition">Delete</button>
                                            </div>
                                            <% } else { %>
                                                <a href="/user/<%= u.discordId %>"
                                                    class="text-blue-400 hover:text-blue-300 transition">View</a>
                                                <% } %>
                                    </td>
                                </tr>
                                <% }); %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="6" class="px-6 py-8 text-center text-gray-400">No users found
                                            </td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="lg:hidden space-y-4">
            <% if (users.length> 0) { %>
                <% users.forEach(u=> { %>
                    <div class="bg-gray-800 rounded-lg p-4 border border-gray-700">
                        <div class="flex items-start justify-between mb-4">
                            <div class="flex items-center space-x-3">
                                <img src="https://cdn.discordapp.com/avatars/<%= u.discordId %>/<%= u.avatar %>.png"
                                    alt="<%= u.username %>" class="w-12 h-12 rounded-full">
                                <div>
                                    <p class="font-semibold text-base">
                                        <%= u.username %>
                                    </p>
                                    <p class="text-xs text-gray-400">
                                        <%= u.email || 'No email' %>
                                    </p>
                                </div>
                            </div>
                            <% if (u.banned) { %>
                                <span class="px-2 py-1 bg-red-500 rounded text-xs font-semibold">BANNED</span>
                                <% } else { %>
                                    <span class="px-2 py-1 bg-green-500 rounded text-xs font-semibold">ACTIVE</span>
                                    <% } %>
                        </div>

                        <div class="space-y-2 text-sm mb-4">
                            <div class="flex justify-between">
                                <span class="text-gray-400">Discord ID:</span>
                                <code class="text-gray-300 text-xs"><%= u.discordId %></code>
                            </div>
                            <div class="flex justify-between items-center">
                                <span class="text-gray-400">Role:</span>
                                <% if (user.role==='owner' && u.role !=='owner' ) { %>
                                    <select data-user-id="<%= u.discordId %>"
                                        class="role-select px-2 py-1 bg-gray-700 border border-gray-600 rounded text-xs focus:outline-none focus:border-red-400">
                                        <option value="user" <%=u.role==='user' ? 'selected' : '' %>>User</option>
                                        <option value="vip" <%=u.role==='vip' ? 'selected' : '' %>>VIP</option>
                                        <option value="admin" <%=u.role==='admin' ? 'selected' : '' %>>Admin</option>
                                    </select>
                                    <% } else { %>
                                        <span class="px-2 py-1 rounded text-xs font-semibold
                                    <%= u.role === 'owner' ? 'bg-purple-500' : '' %>
                                    <%= u.role === 'admin' ? 'bg-red-500' : '' %>
                                    <%= u.role === 'vip' ? 'bg-yellow-500' : '' %>
                                    <%= u.role === 'user' ? 'bg-gray-600' : '' %>">
                                            <%= u.role.toUpperCase() %>
                                        </span>
                                        <% } %>
                            </div>
                            <div class="flex justify-between">
                                <span class="text-gray-400">Joined:</span>
                                <span class="text-gray-300">
                                    <%= new Date(u.createdAt).toLocaleDateString() %>
                                </span>
                            </div>
                        </div>

                        <% if (u.role !=='owner' && u.discordId !==user.discordId) { %>
                            <div class="flex gap-2 pt-3 border-t border-gray-700">
                                <a href="/user/<%= u.discordId %>"
                                    class="flex-1 text-center px-3 py-2 bg-blue-600 hover:bg-blue-700 rounded text-sm transition">
                                    View Profile
                                </a>
                                <button onclick="toggleBan('<%= u.discordId %>', '<%= u.banned ? 'false' : 'true' %>')"
                                    class="flex-1 px-3 py-2 <%= u.banned ? 'bg-green-600 hover:bg-green-700' : 'bg-yellow-600 hover:bg-yellow-700' %> rounded text-sm transition">
                                    <%= u.banned ? 'Unban' : 'Ban' %>
                                </button>
                                <button onclick="deleteUser('<%= u.discordId %>', '<%= u.username %>')"
                                    class="px-3 py-2 bg-red-600 hover:bg-red-700 rounded text-sm transition">
                                    Delete
                                </button>
                            </div>
                            <% } else { %>
                                <div class="pt-3 border-t border-gray-700">
                                    <a href="/user/<%= u.discordId %>"
                                        class="block text-center px-3 py-2 bg-blue-600 hover:bg-blue-700 rounded text-sm transition">
                                        View Profile
                                    </a>
                                </div>
                                <% } %>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <div class="bg-gray-800 rounded-lg p-8 border border-gray-700 text-center text-gray-400">
                                No users found
                            </div>
                            <% } %>
        </div>

        <% if (totalPages> 1) { %>
            <div class="mt-6 flex flex-wrap justify-center gap-2">
                <% if (currentPage> 1) { %>
                    <a href="?page=<%= currentPage - 1 %>"
                        class="px-3 md:px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg hover:bg-gray-700 transition text-sm">
                        Previous
                    </a>
                    <% } %>

                        <% for (let i=1; i <=totalPages; i++) { %>
                            <% if (i===currentPage) { %>
                                <span class="px-3 md:px-4 py-2 bg-red-500 rounded-lg font-semibold text-sm">
                                    <%= i %>
                                </span>
                                <% } else if (i===1 || i===totalPages || (i>= currentPage - 2 && i <= currentPage + 2))
                                        { %>
                                        <a href="?page=<%= i %>"
                                            class="px-3 md:px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg hover:bg-gray-700 transition text-sm">
                                            <%= i %>
                                        </a>
                                        <% } else if (i===currentPage - 3 || i===currentPage + 3) { %>
                                            <span class="px-2 md:px-4 py-2 text-gray-500 text-sm">...</span>
                                            <% } %>
                                                <% } %>

                                                    <% if (currentPage < totalPages) { %>
                                                        <a href="?page=<%= currentPage + 1 %>"
                                                            class="px-3 md:px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg hover:bg-gray-700 transition text-sm">
                                                            Next
                                                        </a>
                                                        <% } %>
            </div>
            <% } %>
    </div>
    <script src="/js/admin.js"></script>
</body>

</html>